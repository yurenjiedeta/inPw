- Performance区域

```tex
#区域1（带有截图列表）：截图区，每一帧都会截图；
#区域2（Memory内存消耗区，是一个曲线图）：勾选后可以看到内存消耗曲线；
#CUP的在Summary饼图可以看到：Scripting 脚本、Rendering 渲染、Painting 绘制、Loading 加载、ldle 闲置
```

- 一些设置

```tex
点击设置，Capture Settings按钮点击后，可以模拟`CPU`的核数

按下 Command+Shift+P（Mac）或者 Control+Shift+P(Windows, Linux) 打开命令菜单
输入Rendering，点选Show Rendering
在Rendering面板里，激活FPS Meter。FPS实时面板就出现在页面的右上方。
```

